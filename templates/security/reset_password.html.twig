{% extends 'base.html.twig' %}

{% block body %}
<div class="card" style="max-width: 460px; margin: 2rem auto;">
    <h1>{{ 'security.reset_title'|trans }}</h1>
    {{ form_start(form) }}
        <div class="field">{{ form_row(form.plainPassword.first) }}</div>
        <div class="field">{{ form_row(form.plainPassword.second) }}</div>
        <button class="btn" type="submit" style="width: 100%;">{{ 'security.reset_btn'|trans }}</button>
    {{ form_end(form) }}
</div>
{% endblock %}
